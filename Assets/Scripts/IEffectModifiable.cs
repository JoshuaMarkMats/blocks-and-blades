using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IEffectModifiable
{
    /*public enum EffectType
    {
        STATE,
        VALUE,
        TICK
    }

    public struct Effect
    {
        public EffectType effectType;
        public bool state;
        public float duration;
        public float durationTimer;
        public float tickDuration;
        public float tickDurationTimer;
        public float strength;
    }

    public Effect CreateEffect(EffectType effectType)
    {
        return new Effect
        {
            effectType = effectType
        };
    }

    public void DoEffect(ref bool state, ref float strength, ref float tickTimer, ref float tickInterval, ref float timer, ref GameObject effect, int value, float tick, float duration)
    {
        state = true;
        strength = value;
        tickTimer = tickInterval = tick;
        regenTimer = duration;
        healEffect.SetActive(true);
    }*/
}
